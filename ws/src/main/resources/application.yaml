spring:
  datasource:
    username: sa
    generate-unique-name: false
  jpa:
    properties:
      javax:
        persistence:
          validation:
            mode: none
  data:    web:      pageable:        default-page-size: 10
        max-page-size: 100
        
                
#  jpa:
#    hibernate:
#      ddl-auto: update

